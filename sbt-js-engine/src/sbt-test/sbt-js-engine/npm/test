# There should be no node modules as there is no package.json visible.
> web-assets:webNodeModules
-$ exists node_modules/console-browserify/index.js

# Unhide the package json - npm should now be invoked
$ copy-file hidden-package.json package.json
> web-assets:webNodeModules
$ exists node_modules/console-browserify/index.js

# Now ensure that a re-run doesn't cause npm to re-install things in the node_modules folder
$ delete node_modules/console-browserify/index.js
> web-assets:webNodeModules
-$ exists node_modules/console-browserify/index.js

# Now touch the package.json file and expect it to perform an update
$ touch package.json
> web-assets:webNodeModules
$ exists node_modules/console-browserify/index.js
